export enum OrderCountry {
  US = "US",
  UK = "UK",
  CA = "CA"

}

export enum OrderType {
  STANDARD = "STANDARD",
  EXPRESS = "EXPRESS"
}

export enum OrderStatus {
  SUBMITTED, PROCESSED, SHIPPED, DELIVERED, ERROR
}
